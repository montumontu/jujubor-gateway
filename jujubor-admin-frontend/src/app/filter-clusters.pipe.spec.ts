import { FilterClustersPipe } from './filter-clusters.pipe';

describe('FilterClustersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClustersPipe();
    expect(pipe).toBeTruthy();
  });
});
